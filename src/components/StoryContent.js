import { Box } from '@mui/material'
import React from 'react'
 
const StoryContent = ({pageContent},pageUrl,prompt) => {
  return (
    <Box> 
        {pageContent}

    </Box>
  )
}

export default StoryContent
